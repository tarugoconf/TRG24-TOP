<div class="block-text theme is-{{ block.theme }} frame-{{ block.frame }}">
  {{ if block.title }}
    <h2 class="type-title">{{ block.title }}</h2>
  {{ /if }}

  <div class="type-txt">
    {{ block.text |> md }}
  </div>
</div>
